## Instance Format

An instance with `N` jobs is given by a plain text file with `n+2` lines:
- Line 1: Number of machines `m`  
- Line 2: Number of jobs `n`  
- The following `n` lines specify the job processing times `p_i`
